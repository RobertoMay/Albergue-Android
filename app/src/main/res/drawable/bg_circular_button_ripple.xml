<!-- res/drawable/bg_circular_button_ripple.xml -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/white"> <!-- Color del efecto ripple -->
    <item>
        <shape android:shape="oval">
            <solid android:color="@color/b41e46"/> <!-- Color de fondo -->
            <stroke
                android:width="1dp"
                android:color="@color/white"/> <!-- Borde blanco -->
        </shape>
    </item>
</ripple>